# BidBoard (Web)

Local prototype for bids/estimates + award decisions + per-trade start/end dates + project 5% fee rollup.

## Run
1) Install Node 18+
2) In this folder:
   - npm install
   - npm run dev
